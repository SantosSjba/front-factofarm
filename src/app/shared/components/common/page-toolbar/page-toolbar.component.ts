import { Component } from '@angular/core';

/** Fila título (slot) + acciones (slot) bajo las migas de pan. */
@Component({
  selector: 'app-page-toolbar',
  standalone: true,
  templateUrl: './page-toolbar.component.html',
})
export class PageToolbarComponent {}
